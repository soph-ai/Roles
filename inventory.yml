all:
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: '~/.ssh/ansible_id_rsa'
    ansible_ssh_common_args: '-o StrictHostKeyChecking=no'
  children:
  # apply common role to dev server 
    devServer:
      hosts:
        172.31.7.229: 
  # apply common and webserver role to webserver 
    webServer:
      hosts: 
         172.31.0.11: 
      